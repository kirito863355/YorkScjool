@model YorkScjool.Models.Lesson

<h2>Delete Lesson</h2>

<p>Are you sure you want to delete "@Model.Title"?</p>

@using (Html.BeginForm("DeleteConfirmed", "Lesson", FormMethod.Post))
{
    @Html.HiddenFor(m => m.Id)
    <button type="submit">Delete</button>
}
